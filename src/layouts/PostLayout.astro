---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  description: string;
  pubDate: Date;
  title: string;
  updatedDate?: Date;
}

const { description, pubDate, title, updatedDate } = Astro.props;

const dateFormatter = new Intl.DateTimeFormat("en", {
  day: "numeric",
  month: "long",
  year: "numeric",
});
---

<BaseLayout title={`${title} | aplysaline`} description={description}>
  <article class="post">
    <header class="post-header">
      <h1>{title}</h1>
      <p class="lede">{description}</p>
      <p class="meta">
        <time datetime={pubDate.toISOString()}>{dateFormatter.format(pubDate)}</time>
        {updatedDate && (
          <>
            <span aria-hidden="true"> · </span>
            <span>Updated {dateFormatter.format(updatedDate)}</span>
          </>
        )}
      </p>
    </header>
    <div class="post-body">
      <slot />
    </div>
  </article>
</BaseLayout>
